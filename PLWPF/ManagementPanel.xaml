<UserControl x:Class="PLWPF.ManagementPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" FlowDirection="RightToLeft">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <TextBlock Text="שינוי סיסמת מנהל:" FontSize="18"/>
            
        </StackPanel>
        <StackPanel Name="FieldsPanel" Grid.Column="1" DataContext="Configu">
            <TextBox Name="ne" Text="הזן סיסמה חדשה" MouseMove="TextBox_MouseMove" FontSize="18"/>
            <PasswordBox Name="MasterPass" BorderThickness="2,2,2,2" />
        </StackPanel>

        <Button Content="עדכן" Click="Button_Click" Grid.Row="1" Width="auto" Height="auto" HorizontalAlignment="Center" Grid.ColumnSpan="2" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
