<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:BE="clr-namespace:BE;assembly=BE" 
             x:Class="PLWPF.checkings"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded">



    <UserControl.Resources>
        <CollectionViewSource x:Key="addressViewSource" d:DesignSource="{d:DesignInstance {x:Type BE:Address}, CreateList=True}"/>
    </UserControl.Resources>



    <Grid DataContext="{StaticResource addressViewSource}">


        <DataGrid x:Name="addressDataGrid" RowDetailsVisibilityMode="VisibleWhenSelected" Margin="249,77,151,173" ItemsSource="{Binding}" EnableRowVirtualization="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="buildingNumberColumn" Width="SizeToHeader" Header="Building Number" Binding="{Binding BuildingNumber}"/>
                <DataGridTextColumn x:Name="cityColumn" Width="SizeToHeader" Header="city" Binding="{Binding city}"/>
                <DataGridTextColumn x:Name="streetColumn" Width="SizeToHeader" Header="Street" Binding="{Binding Street}"/>
            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</UserControl>
